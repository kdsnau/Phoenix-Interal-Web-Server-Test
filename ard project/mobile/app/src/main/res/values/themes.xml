<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme; Compose drives the actual colors. Keep it a Material3 parent
         with no action bar so the Compose UI owns the whole surface. -->
    <style name="Theme.PhxDoor" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">#0E1116</item>
        <item name="android:windowBackground">#0E1116</item>
    </style>
</resources>
